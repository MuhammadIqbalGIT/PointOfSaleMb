<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:models="using:App1.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <ListView x:Name="ProductList">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <StackPanel Margin="5">
                        <TextBlock Text="ID:" FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Bind Id}" FontSize="14"/>
                        <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                        <TextBlock Text="{x:Bind Name}" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="Price:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                        <TextBlock Text="{x:Bind Price, Mode=OneWay}" FontSize="14" Foreground="Green"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
